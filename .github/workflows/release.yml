name: Release
on:
  push:
    branches:
      - release
    paths:
      - "**/Cargo.toml"
      - ".github/workflows/release.yml"

jobs:
  publish:
    runs-on: ubuntu-22.04
    strategy:
      fail-fast: false
      max-parallel: 1
      matrix:
        package:
          - name: asynk-grafql-value
            registryName: asynk-grafql-value
            path: value
          - name: asynk-grafql-parser
            registryName: asynk-grafql-parser
            path: parser
          - name: asynk-grafql-derive
            registryName: asynk-grafql-derive
            path: derive
          - name: asynk-grafql
            registryName: asynk-grafql
            path: .
          - name: asynk-grafql-extras
            registryName: asynk-grafql-extras
            path: extras
          - name: asynk-grafql-actix-web
            registryName: asynk-grafql-actix-web
            path: integrations/actix-web
          - name: asynk-grafql-warp
            registryName: asynk-grafql-warp
            path: integrations/warp
          - name: asynk-grafql-poem
            registryName: asynk-grafql-poem
            path: integrations/poem
          - name: asynk-grafql-axum
            registryName: asynk-grafql-axum
            path: integrations/axum
          - name: asynk-grafql-rocket
            registryName: asynk-grafql-rocket
            path: integrations/rocket
    steps:
      - uses: dtolnay/rust-toolchain@stable
        with:
          toolchain: stable
      - name: Checkout
        uses: actions/checkout@v4
      - name: get version
        working-directory: ${{ matrix.package.path }}
        run: echo PACKAGE_VERSION=$(sed -nE 's/^\s*version = "(.*?)"/\1/p' Cargo.toml) >> $GITHUB_ENV
      - name: check published version
        run: echo PUBLISHED_VERSION=$(cargo search ${{ matrix.package.registryName }} --limit 1 | sed -nE 's/^[^"]*"//; s/".*//1p' -) >> $GITHUB_ENV
      - name: cargo login
        if: env.PACKAGE_VERSION != env.PUBLISHED_VERSION
        run: cargo login ${{ secrets.CRATES_TOKEN }}
      - name: cargo package
        if: env.PACKAGE_VERSION != env.PUBLISHED_VERSION
        working-directory: ${{ matrix.package.path }}
        run: |
          cargo package
          echo "We will publish:" $PACKAGE_VERSION
          echo "This is current latest:" $PUBLISHED_VERSION
      - name: Publish ${{ matrix.package.name }}
        if: env.PACKAGE_VERSION != env.PUBLISHED_VERSION
        working-directory: ${{ matrix.package.path }}
        run: |
          echo "# Cargo Publish"
          cargo publish --no-verify
